*** Begin Patch
*** Add File: probe.rs
+fn compute(a: i32, b: i32, c: i32, d: i32, e: i32) -> i32 {
+    let sum = a +b + c + d + e;
+    sum
+}
*** Add File: probe.py
+def compute(a, b, c, d, e):
+    value = a +b + c + d + e
+    return value
*** End Patch
